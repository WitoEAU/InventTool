@model InventTool.BL.Categoria

@{
    ViewBag.Title = "Eliminar";
}

<h2></h2>

<b style="font-size: large">Esta seguro que quiere eliminar esta categoria?</b  >
    <div>
        <h4></h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Descripcion)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Descripcion)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <br>

            <p class="form-actions no-color">
                <input type="submit" value="Eliminar" class="btn btn-default btn-sm" style="color: #3366BB " />
            </p>

            <p class="btn btn-default btn-sm" , style="color: #3366BB;">
                @Html.ActionLink("Regresar a la lista", "Index")
            </p>
        }
    </div>
